# Reproducibility / Traceability Audit

Audit date: 2026-06-05
Draft: `corruption_root_cause.tex`

## Verdict

**FAIL on archive-wide claim traceability; PASS on the demonstrated source
mechanism, eight-shard control figures, figure regeneration, and LaTeX
compilation.**

The source-history mechanism and the `res_8pc/phase2/00028` eight-shard
fingerprint are reproducible from surviving artifacts. The stronger statement
that all five simulations were checked and twelve of fourteen streams are
affected is stated in `PDF_OUTPUT_CORRUPTION_SUMMARY.md`, but the completed raw
audit product cited by that summary is absent. The preserved full-audit jobs
were canceled or failed. That scope claim therefore remains unsupported by a
surviving completed audit artifact.

The checked-in `corruption_root_cause.pdf` is also older than the current
`.tex` source. The current source compiles successfully to temporary storage,
but the PDF beside it must be regenerated before release.

## Figure And Input Traceability

| Item | Status | Trace |
|---|---|---|
| `figures/output29_corruption_signature.pdf` and `.png` | PASS | Files exist. Generated by `docs/explanatory_writeups/scripts/generate_figures.py`. |
| `figures/archived_control_errors.pdf` and `.png` | PASS | Files exist. Generated by the same script. |
| Quantitative metrics | PASS | `docs/explanatory_writeups/figure_metrics.json` records the exact metrics, reader, rebuilt payloads, archived headers, and eight archived sparse shards for both `output29` and `output31`. |
| Documented figure inputs exist | PASS | All 32 unique paths recorded by `figure_metrics.json` exist. |
| Lightweight regeneration | PASS for scientific content | Two staged regenerations reproduced the `comparisons` section exactly. The current PNGs are pixel-identical to staged output. Generated target files changed concurrently during the audit, so byte-level no-overwrite cannot be certified for the document artifacts. |
| Source-history trace | PASS | `git show` verifies introduction at `ec009a27b87029db6f35a95045ae624ff5c0d1e4` and removal at `db953863b6c0e06d893b4a9738d4c111279e42fe`. Current `src/outputs/derived_variables.cpp` has no inner `Kokkos::realloc` in `coord_abscostheta`. |
| Archive-wide radius audit output | FAIL | Expected files `analysis/catalog/generated/pdf_coord_r_audit/full/pdf_coord_r_audit.csv` and `pdf_coord_r_audit_summary.md` do not exist. Jobs `3318065` and `3318066` were canceled; job `3318069` failed on a partition-manifest mismatch. |

## Claim Traceability

Supported:

- The destructive allocation was introduced and later removed at the stated
  commits.
- The eight-shard rebuilt `output29` full distribution disagrees strongly with
  the archive while its radial marginal agrees.
- The eight-shard rebuilt `output31` controls agree closely.
- The signed totals remain close for the matched subset.
- The document labels the eight-shard comparison as partial and does not claim
  that it proves complete AMR closure.

Unsupported or incompletely preserved:

- **"Observed across all five simulations; twelve of fourteen streams
  affected."** The summary states this, but no completed CSV/Markdown audit
  product survives and the preserved full-audit logs do not complete
  successfully.
- The archive-wide forms of "`output31` remains useful" and "the twelve broken
  streams need reconstruction" inherit the same missing completed-audit
  artifact. The source mechanism strongly supports them, but the current
  trace does not independently preserve the all-simulation measurement.

Required resolution: preserve a successful latest-snapshot or full archive
radius-audit CSV and summary, record its exact command and job result, and link
it from this draft or its evidence notes.

## Compilation

Status: **PASS with layout warnings; checked-in PDF stale.**

The current source compiled twice with `pdflatex -halt-on-error` to temporary
storage and produced a five-page PDF. The final log had no fatal errors, one
overfull box, and ten underfull boxes. At audit time:

- `corruption_root_cause.tex` modification time: `2026-06-05 07:49:02 -0400`
- checked-in `corruption_root_cause.pdf` modification time:
  `2026-06-05 07:39:19 -0400`

## No-Overwrite And Resource Check

- PASS: figure regeneration and document compilation wrote only beneath
  `/tmp/gotham-repro-audit.T5cDY9`.
- PASS: both staged regenerations wrote only to temporary storage and reproduced
  the scientific metrics exactly.
- INDETERMINATE: generated target figure files changed concurrently during the
  audit window. The current PNG pixels reproduce exactly from the current
  script, but this auditor cannot certify that no generated document artifact
  was overwritten by another process.
- PASS: archived sparse PDFs, rebuilt products, logs, manifests, and source
  history were read only.
- PASS: no Slurm job, Frontier job, external API, or image-generation service
  was used by this audit.
- PARTIAL: `docs/explanatory_writeups/RESOURCE_LEDGER.md` accurately records the
  original lightweight figure and TeX work, but it does not record this audit's
  local synthetic test run, temporary CPU comparison, or the concurrent target
  figure regeneration. Those observations are recorded here because this
  auditor was instructed not to edit other files.

## Commands Attempted

**PASS: staged figure regeneration**

```bash
mkdir -p /tmp/gotham-repro-audit.T5cDY9/docs/explanatory_writeups/scripts
cp docs/explanatory_writeups/scripts/generate_figures.py \
  /tmp/gotham-repro-audit.T5cDY9/docs/explanatory_writeups/scripts/
ln -s /ccs/home/dfielding/athenak-gotham-pdf-rebuild/tools \
  /tmp/gotham-repro-audit.T5cDY9/tools
module load python/3.7-anaconda3
PYTHONPATH=/lustre/orion/ast207/proj-shared/gotham/analysis \
python /tmp/gotham-repro-audit.T5cDY9/docs/explanatory_writeups/scripts/generate_figures.py
```

Result: all figures generated; scientific metrics exactly matched
`docs/explanatory_writeups/figure_metrics.json`.

**PASS: documented input existence**

```bash
python - <<'PY'
# Load figure_metrics.json and test every recorded input path with Path.exists().
PY
```

Result: `32` documented paths, `32` unique paths, `0` missing.

**PASS: source-history verification**

```bash
git show ec009a27b87029db6f35a95045ae624ff5c0d1e4 -- src/outputs/derived_variables.cpp
git show db953863b6c0e06d893b4a9738d4c111279e42fe -- src/outputs/derived_variables.cpp
```

Result: the first commit makes the allocation unconditional; the second
removes it.

**FAIL: locate completed archive-wide radius audit products**

```bash
stat /lustre/orion/ast207/proj-shared/gotham/analysis/catalog/generated/pdf_coord_r_audit/full/pdf_coord_r_audit.csv
stat /lustre/orion/ast207/proj-shared/gotham/analysis/catalog/generated/pdf_coord_r_audit/full/pdf_coord_r_audit_summary.md
```

Result: both files missing. Preserved job logs show cancellation or failure.

**PASS: current-source compilation**

```bash
pdflatex -interaction=nonstopmode -halt-on-error \
  -output-directory=/tmp/gotham-repro-audit.T5cDY9/compile-latest/corruption_root_cause \
  corruption_root_cause.tex
```

Run twice from the document directory. Result: five-page PDF, no fatal error.

## Pass-Two Follow-up

After this audit, the quantitative figures were regenerated from the final
script and the checked-in six-page document was compiled twice from the revised
source.
The document now labels the archive-wide twelve-of-fourteen scope as reported
rather than independently reproduced. The missing completed archive-wide
audit and same-state bad-line/fixed-line AthenaK A/B run remain open.
